# Task 19: Schema Browser Tree Component

## Implementation Details
- Created `SchemaTree.kt` containing the `SchemaTree` and `SchemaTreeNodeItem` composables.
- Implemented a collapsible/expandable hierarchy mapping to a domain model (`SchemaTreeNode`) using Compose's `LazyColumn`.
- Supported proper desktop right-click via experimental `onClick(matcher = PointerMatcher.mouse(PointerButton.Secondary))`.
- Updated to pass context menu actions (`onCopyName`, `onViewData`) cleanly via lambda callbacks rather than hardcoding side effects (println) deep in the UI components.
- Wired the placeholder mock schema into `App.kt`.

## Notes
- As this development environment is headless, traditional visual PNG screenshots of the Compose UI are unfeasible. Text-based confirmation of compilation serves as our functional evidence.

## Verification
`./gradlew :app:compileKotlin` completes successfully with no unresolved errors.

```
> Task :app:compileKotlin
w: file:///home/jonathan/desarrollo/db-eagle/app/src/main/kotlin/com/dbeagle/ui/ConnectionDialog.kt:57:45 'fun Modifier.menuAnchor(): Modifier' is deprecated. Use overload that takes MenuAnchorType and enabled parameters.

BUILD SUCCESSFUL in 3s
5 actionable tasks: 1 executed, 4 up-to-date
```
