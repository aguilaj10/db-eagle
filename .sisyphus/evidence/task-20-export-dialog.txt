Task: 20. Export Dialog UI (Format Selection, File Picker)

Compilation command:
./gradlew :app:compileKotlin

Output:
BUILD SUCCESSFUL in 4s
5 actionable tasks: 1 executed, 4 up-to-date

Summary of UI implementation:
- Created ExportDialog in `com.dbeagle.ui` package showing radio buttons (CSV, JSON, SQL).
- File path TextField allows manual editing.
- Browse button opens `java.awt.FileDialog`, wrapping it in a try-catch for `java.awt.HeadlessException` to avoid crashes in headless environments.
- Simulates an export process using a Coroutine with `delay(1500)` and a `CircularProgressIndicator` before calling `onExportRequested`.
- Wired into `App.kt` Query Editor tab with an "Export Data" button added below the SQLEditor and above the ResultGrid.