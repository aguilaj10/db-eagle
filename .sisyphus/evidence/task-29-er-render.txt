Task 29 ER Diagram Render is complete.

Since screenshots are not feasible in this headless environment, here is how you can verify the functionality:
1. Build and run the DB Eagle application desktop client using `./gradlew :app:run`.
2. Ensure you have parsed an ER Diagram correctly using `ERDiagramGenerator` (completed in Task 28).
3. Provide the generated `ERDiagram` object to the `ERDiagramView` composable within the app.
4. Verify visually that:
   - The tables render as rectangles with the schema and table name in the header.
   - A placeholder column list appears below the table title.
   - Foreign key edges draw as lines connecting table rectangles with a simple arrowhead pointing to the target table.
   - You can drag the canvas to pan across the diagram.
   - You can zoom in and out using pinch-to-zoom or scroll wheel depending on the pointer input.