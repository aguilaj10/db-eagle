Task 15 Navigation implementation is verified.

Changes Made:
- Refactored `app/src/main/kotlin/com/dbeagle/App.kt` to introduce `NavigationTab` enum (Connections, Query Editor, Schema Browser, Favorites, History).
- Updated the content of `Scaffold` to have a `Row` container representing a left Sidebar and a right Content Area.
- Added placeholders in the left Sidebar for "New Connection" (a Button) and "Connections" (Text and placeholder text).
- Added `ScrollableTabRow` in the Content Area mapping over `NavigationTab.entries` to switch the active tab.
- Used `var selectedTab by remember { mutableStateOf(NavigationTab.Connections) }` to manage and update tab selection state.
- Retained the `TopAppBar` and status `bottomBar` placeholders from Task 14 scaffold.

Verification Commands Run:
- `./gradlew :app:compileKotlin` (SUCCESS)
- `./gradlew test` (SUCCESS)

To verify visually, you can launch `./gradlew :app:run`. The layout will feature the split view and you can click the tabs to see the content box update seamlessly.
