Task 35: Keyboard Shortcuts + Accessibility Basics

Implementation Details:
- **Ctrl/Cmd+Enter**: Added `onPreviewKeyEvent` handler to the root `Column` of `SQLEditor.kt` to trigger the `onRun` callback if not already running. (app/src/main/kotlin/com/dbeagle/ui/SQLEditor.kt:53)
- **Ctrl/Cmd+N**: Handled in the global `Window` `onPreviewKeyEvent` in `App.kt` to select the `NavigationTab.Connections` and set `triggerNewConnection = true`. Passed down to `ConnectionManagerScreen` and `ConnectionListScreen` to automatically trigger the new connection dialog. Added equivalent wiring to the placeholder "New Connection" button. (app/src/main/kotlin/com/dbeagle/App.kt:108)
- **Ctrl/Cmd+W**: Handled in global `Window` `onPreviewKeyEvent` in `App.kt`, closing the currently active session tab via `sessionViewModel.closeSession(activeProfileId)`. (app/src/main/kotlin/com/dbeagle/App.kt:113)
- **Ctrl/Cmd+,**: Handled in global `Window` `onPreviewKeyEvent` in `App.kt`, setting the active tab to `NavigationTab.Settings`. (app/src/main/kotlin/com/dbeagle/App.kt:120)
- **Accessibility/Focus**: Added a `.border` to the wrapper `Box` of `CodeEditText` within `SQLEditor.kt` that changes color and thickness based on `isFocused` state via `onFocusChanged`. (app/src/main/kotlin/com/dbeagle/ui/SQLEditor.kt:119)

Verification:
- `./gradlew :app:compileKotlin` completed successfully.
- `./gradlew test` completed successfully.
- Cross-platform support added through `isCtrlPressed || isMetaPressed`. Repeated execution blocked in Ctrl+Enter by guarding with `!isRunning`.
- Changed App.kt keyboard shortcuts to use KeyEventType.KeyUp to avoid continuous triggering from OS key repeats.
- Changed SQLEditor.kt Run shortcut to use KeyEventType.KeyUp.
- Adjusted SQLEditor.kt focus modifier by moving `onFocusChanged` down the modifier chain to sit on the inner `CodeEditText` wrapper or simply adjusting order so the focus border updates correctly.
